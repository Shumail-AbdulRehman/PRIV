# CleanOps — Project Context & Handoff

Consolidated project context. Updated: 2026-09-03.
Covers: architecture, data model, API, deploy, the QR + area-match
verification stack, migration runbook, known issues, safe commands.

================================================================================

## 1. WHAT THIS PROJECT IS

CleanOps is a multi-tenant cleaning / facility-operations management SaaS
(FYP). Business flow:

- A company signs up -> gets ONE ADMIN account (the company owner, web
  dashboard only).
- Admin creates locations (lat/long, geofence radius, IANA timezone
  auto-detected from coordinates) and managers. Each manager must be assigned
  >= 1 location; a location can belong to ONLY ONE manager (exclusive
  assignment, DB-enforced).
- Admin/managers create staff (mobile users with shift windows) and task
  templates (DAILY or ONCE recurrence, each with a unique QR token).
- Backend crons generate daily task instances from templates, auto-assign
  them to the best eligible staff, auto-reassign tasks not started within a
  grace period, and mark missed work.
- Staff use the Expo mobile app: login -> geofenced selfie check-in/out ->
  view today's tasks -> scan the location QR to start a task -> per-area QR
  re-scan + camera-only photo within a 90s server-enforced window -> each
  photo is area-match verified (Diff All API) before the area counts as done.
- Attendance and task outcomes roll up into manager dashboards (Today Status,
  location stats, staff detail), including area-match pass/flag/block results
  with staff-vs-reference photo comparison.

================================================================================

## 2. REPO LAYOUT & STACK

Monorepo at `/home/shumail/bk-Priv/PRIV`:

- `backend/` — Node 22 (nvm v24.14 also used locally), Express 5,
  TypeScript ESM (NodeNext), Prisma 7 + `@prisma/adapter-pg`, PostgreSQL
  (Supabase), Zod 4, node-cron, date-fns + date-fns-tz, tz-lookup, Cloudinary,
  multer (memoryStorage), bcrypt, Vitest (34 tests). API on port 8080.
  `npm run dev` = `tsc -b --watch` + `nodemon dist/app.js` — it runs the
  COMPILED dist; restart if dist seems stale.
- `frontend/` — React 19, Vite 8 (React Compiler enabled), Tailwind CSS 4,
  Redux Toolkit, TanStack Query, React Router 7, shadcn-style UI, Recharts,
  react-hook-form. Dev on 127.0.0.1:5173 with Vite proxy `/api -> :8080`.
- `frontend-mobile/` — Expo 54, RN 0.81, React Navigation (native-stack),
  TanStack Query, axios, expo-camera / expo-location / expo-image-picker.
  Staff-only app; also has a web export (`npm run build:web`).

================================================================================

## 3. CORE ARCHITECTURE

### Roles & tenancy

- Prisma `Role` enum: ADMIN | MANAGER | STAFF.
- ADMIN and MANAGER logins both live in the `Manager` table; staff login
  lives in the `Staff` table. Company is the tenant root.
- `verifyJwt` (backend/src/middlewares/auth.middleware.ts) accepts the
  `accessToken` cookie or `Authorization: Bearer`; loads the user, rejects
  inactive MANAGER/ADMIN accounts, and attaches `req.user.locationIds` for
  MANAGER (from `ManagerLocation`).
- `authorize(...roles)` middleware factory guards routes.
- `backend/src/utils/scope.ts`: `getScopedLocationIds` (null = ADMIN
  unrestricted) and `assertLocationAccess` (403 for out-of-scope MANAGER).
  All location/staff/task/attendance controllers enforce this.

### Auth tokens

- JWT access (default 15m) + refresh (default 7d). Refresh flow
  (`POST /api/common/refresh-token`) rotates both tokens, stores the refresh
  token server-side, and has REUSE DETECTION (mismatched stored token ->
  null it out + 403).
- Cookies configurable via `COOKIE_SECURE` / `COOKIE_SAME_SITE`; path `/`.
- Logout works without a valid access token (clears cookies best-effort).
- Passwords hashed by a Prisma client extension (bcrypt, 10 rounds) in
  `backend/src/prisma/prisma.ts` — invisible at call sites, keep in mind.

### Timezone model (IMPORTANT)

- `Location.timezone` = IANA name, default "Asia/Karachi" for existing rows.
- Every DB DateTime is a true UTC instant; wall-clock meaning comes from the
  owning location's timezone (09:00 at a Karachi location = 04:00Z).
- Day anchors (`TaskInstance.date`, `Attendance.date`) = start of the
  location's LOCAL day as a UTC instant (Karachi: 19:00Z previous UTC day).
- `backend/src/utils/dateTime.ts` holds the zoned helpers
  (`getZonedDayRange`, `getZonedDayRangeFromDateInput`, `getZonedMonthRange`,
  `getZonedClockMinutes`, `withZonedClockOnLocalDay`,
  `resolveZonedAttendanceWindow`, `isValidTimeZone`) plus legacy plain-UTC
  helpers. Location-scoped logic MUST use the zoned helpers (DST-safe).
- Timezone auto-detection: createLocation/editLocation derive tz from
  lat/long via tz-lookup when not explicitly provided (fallback "UTC").
- Web frontend formats per-record location tz via formatInTimeZone; time
  inputs convert via fromZonedTime; calendar-date-only values stay UTC.
- Mobile app: all date logic is still HARDCODED to Asia/Karachi
  (utils/attendance.ts, utils/format.ts) — known gap.

### Task lifecycle

- `TaskTemplate` (recurringType DAILY|ONCE, effectiveDate,
  recurringEndDate?, unique qrToken) ->
  crons create `TaskInstance` rows (`@@unique([templateId,date])` makes
  generation idempotent) ->
  `services/taskAssignment.service.ts` assigns: prefers template-designated
  staff, else `findBestStaffForTask` scores candidates by shift coverage
  (overnight-normalized), attendance eligibility, and current workload;
  race-safe transactions; everything written to `AuditLog`.
- Staff starts a task via QR scan (`POST /api/task-instance/:id/start?qrToken=`)
  and completes with >= 1 proof image (max 5, Cloudinary) — see sections
  12-13 for the QR + area verification flow that now governs multi-area tasks.
- Statuses: PENDING / IN_PROGRESS / COMPLETED / NOT_COMPLETED_INTIME /
  MISSED / CANCELLED. Assignment statuses tracked separately in
  `TaskAssignment` with `isCurrent` history.
- Reassignment cron (every minute) reassigns ASSIGNED tasks past
  `TASK_START_GRACE_MINUTES` (default 10), max `MAX_TASK_REASSIGNMENTS`
  (default 3).

### Attendance

- Crons create daily ABSENT rows per staff (`@@unique([staffId,date])`).
- Check-in: haversine geofence vs `location.radiusMeters` (server-side),
  30-min early window, 15-min late grace, selfie to Cloudinary, atomic
  `updateMany` guarded on status ABSENT.
- Check-out: geofence, 30-min minimum shift duration, late checkout keeps
  MISSED_CHECKOUT status. Statuses: ABSENT / CHECKED_IN / CHECKED_OUT /
  LATE / MISSED_CHECKOUT.

### Cron jobs (backend/src/cron/, imported for side effects in app.ts)

- `dailyTaskScheduler` — runs at import AND every 90s via setInterval
  (KNOWN ISSUE: wasteful; should be per-location-midnight scheduling).
- `onceTaskScheduler` — node-cron `3-59/15 * * * *`.
- `taskStatusCron` — `7-59/15 * * * *`; expired PENDING -> MISSED,
  expired IN_PROGRESS -> NOT_COMPLETED_INTIME.
- `taskReassignmentCron` — every minute.
- `attendanceCron` — `2 */6 * * *` (every 6h); creates ABSENT rows +
  syncs open attendance windows (sequential loop — Promise.all was removed
  after Prisma pool P1017 connection deaths).
- `attendanceStatusCron` — `5-59/10 * * * *`; past expectedEnd ->
  MISSED_CHECKOUT.
- `startupCron` — catch-up superset, awaited on boot.

================================================================================

## 4. DATA MODEL (Prisma, backend/prisma/schema.prisma)

- `Company` — tenant root.
- `Manager` — unique email, role (ADMIN/MANAGER/STAFF), refreshToken,
  company FK, assignedLocations via ManagerLocation.
- `Staff` — unique email, phone?, shiftStart/shiftEnd (wall-clock only),
  nullable locationId (SetNull), refreshToken; indexes on
  companyId/locationId/isActive.
- `Location` — latitude/longitude stored as STRING, radiusMeters (default
  500), timezone (IANA).
- `TaskTemplate` — title/desc, optional staffId, location FK, shift window,
  recurrence, unique qrToken. Legacy `referenceImageUrl` kept for backwards
  compatibility; new templates use `referenceImages TaskTemplateReferenceImage[]`.
- `TaskInstance` — denormalized template copy, date = local-day anchor,
  status, isLate/lateMinutes, startedAt/completedAt, proofImageUrls[];
  `@@unique([templateId,date])` + composite indexes. Legacy `referenceImageUrl`
  kept; new instances use `referenceImages TaskInstanceReferenceImage[]`.
- `TaskTemplateReferenceImage` / `TaskInstanceReferenceImage` — name, imageUrl,
  sortOrder, cascade delete with parent. "Area" == one reference image row;
  multiple rows with the SAME name are treated as camera angles of one area.
- `TaskAreaSubmission` (2026-09) — one row per (taskInstanceId,
  referenceImageId), `@@unique` enforced. Tracks the QR-scan/photo/verify
  lifecycle per area:
    scannedAt, uploadedAt, photoUrl,
    status SubmissionStatus = PENDING | APPROVED | REJECTED_TIMEOUT |
      BLOCKED | CV_ERROR,
    similarityScore, areaMatchStatus ("passed"|"flagged"|"blocked"),
    areaMatchFlag, bestReferenceImageId,
    colorScore/ssimScore/featureScore (NULL under Diff All — single score),
    matchThresholdUsed/blockThresholdUsed/colorWeightUsed/ssimWeightUsed/
      featureWeightUsed (audit of the exact config at decision time),
    attempts Json (append-only audit log of EVERY photo attempt, including
      blocked retries and cv_error).
- `TaskAssignment` — per-instance assignment history (status, reason,
  isCurrent, lifecycle timestamps, Cascade delete).
- `TaskCompletionAttempt` — legacy Gemini-era verification attempts
  (locationMatchScore/cleanlinessMatchScore/rawResponse); still written by
  the legacy single-image completion path.
- `Attendance` — unique [staffId,date], expectedStart/End, check-in/out
  times + selfie URLs, late fields.
- `AuditLog` — companyId?, actorType/actorId, entityType/entityId, action,
  reason, old/new Json.
- `ManagerLocation` — composite PK [managerId,locationId],
  `locationId @unique` (one manager per location).

MIGRATION HISTORY (2026-09-02): squashed to a SINGLE baseline
`20260902120000_baseline` (full current schema). The old 25-migration chain
was unrecoverable (past `db push` edits never had migration files, so the
chain could not replay even on a fresh DB — `20260812150000` referenced a
table no earlier migration created). `_prisma_migrations` was cleaned and the
baseline marked applied. See section 8 for the runbook.

================================================================================

## 5. API SURFACE (all under /api)

- `manager`: POST /manager-signup, POST /manager-login (public);
  GET /profile/me; GET /today-status (ADMIN,MANAGER);
  POST /, GET /, PATCH /:id (ADMIN only — create/list/update managers,
  409 on location-assignment conflict).
- `staff`: POST /staff-login (public); CRUD + /inactive + /details/:id +
  /location/:locationId (ADMIN,MANAGER, scoped); GET /profile/me.
- `location`: create/edit/deactivate/inactive/restore (ADMIN only);
  GET /, GET /:id, GET /:id/stats (ADMIN,MANAGER).
- `task-template`: CRUD + GET /location/:locationId (ADMIN,MANAGER).
- `task-instance`:
  GET /staff/:staffId/today,
  POST /:taskId/start?qrToken=,
  POST /:taskId/area/:referenceImageId/scan (per-area QR re-scan),
  POST /:taskId/area/:referenceImageId/upload (multer single "photo";
  runs area-match verification, returns verdict),
  GET /:taskId/area-submissions (manager/staff: full per-area photos,
  scores, match status, attempt history),
  POST /:taskId/complete (multer, max 5 images — legacy path),
  GET /:taskId, GET /location/:locationId.
- `assignment`: PATCH /staff/:staffId/location/:locationId,
  PATCH /task-template/:templateId/staff/:staffId.
- `attendance`: PATCH /staff/:id/shift, POST /check-in, POST /check-out
  (multer selfie), GET /my, GET / (ADMIN,MANAGER).
- `common`: POST /logout, POST /refresh-token (unauthenticated by design),
  GET /get-current-user.

Response envelope: ApiResponse `{statusCode,data,message,success}` /
ApiError `{success:false,message,errors}`. Zod v4 schemas in
backend/src/validations/.

================================================================================

## 6. FRONTEND (web) NOTES

- Redux holds ONLY authSlice `{user,isAuthenticated,isLoading}`; all server
  state in TanStack Query (global: staleTime 60s, gcTime 10min, no
  refetchOnFocus/Reconnect, retry 1; per-query tuning on dashboard/
  attendance/today-status/staff; hover-prefetch of staff details).
- Routes: / (Landing, public), /features (public), /signup, /login
  (GuestRoute + share PublicNavbar), /dashboard, /today-status,
  /locations, /locations/:id, /staff, /staff/:id, /attendance
  (ProtectedRoute), /managers (RequireRole ADMIN). NO 404 catch-all route.
- axios client (src/api/client.ts): baseURL `VITE_API_BASE_URL` (default
  `/api`), withCredentials, single-flight refresh on 401 with exact body
  message "Access Token Expired".
- Per-feature convention: `api.ts` + `queries.ts` + page + components.
- UI: shadcn-style primitives, Tailwind 4 CSS-first theme, Geist font,
  oklch teal palette aligned with the mobile app primary
  (oklch(0.52 0.13 190)); dark variant defined but no toggle.
- AREA-MATCH PANEL (2026-09): LocationDetailPage -> "Task Instances" tab
  has an "Areas" expandable column per instance rendering
  `frontend/src/pages/Task/components/AreaSubmissionsPanel.tsx`
  (staff-vs-reference thumbnails, "Area Match: Passed / Needs Review /
  Failed" badge, similarity %, timestamp, per-attempt history). Data via
  `getAreaSubmissions(taskId)` in frontend/src/pages/Task/api.ts +
  `useGetAreaSubmissions` in queries.ts.

## 7. MOBILE APP NOTES

- Single native-stack; auth-gated: Login vs (StaffTabs + QrScanner +
  CompleteTask). "Tabs" are a hand-rolled two-button bar (Shift / Tasks),
  not bottom-tabs.
- Session (user + both tokens) persisted as JSON in AsyncStorage key
  `@cleanops-staff/auth` (NOT SecureStore — known security gap).
- axios bridge pattern (`configureApiAuth`); refresh on 401 string match
  "Access Token Expired"; multipart uploads use raw fetch with a 20s
  AbortController timeout (`uploadFormData` in src/api/client.ts).
- Geofence enforced SERVER-side; app just sends lat/long from
  expo-location. No push notifications.
- API base: `EXPO_PUBLIC_API_BASE_URL`. As of 2026-09-03 it points at the
  LAN IP `http://192.168.100.37:8080/api` (in frontend-mobile/.env) — the
  IP changes per network; update it when testing on a new Wi-Fi. Phone must
  be on the same network; firewall may need port 8080 open.
- No EAS config (no eas.json); app.json allows cleartext HTTP on both
  platforms.
- NativeWind + Reanimated v4 setup: `nativewind` pinned to exact `4.1.23`,
  `react-native-reanimated ~4.1.x`, `react-native-worklets ~0.5.x`.
  `babel-preset-expo` and `react-refresh` are explicit dev dependencies.
- Tailwind animation classes are NOT used on plain Views; skeleton/spinner
  use React Native's built-in `Animated` API.
- QR SCANNER (2026-09-03, frontend-mobile/src/screens/QrScannerScreen.tsx):
  the scan-frame area is fully transparent and only the screen OUTSIDE the
  frame is dimmed (a full-screen dim overlay previously made the camera
  look dark); torch toggle button; barcodeScannerSettings is a module-level
  constant and the scan handler is a stable useCallback with a ref guard
  (prop changes caused dark-preview reconfigurations on Android); camera
  mounts 350ms after focus (mid-animation mounts also caused dark previews)
  and a 4s watchdog force-remounts the surface if onCameraReady never fires.
- CompleteTaskScreen is a per-area stepper (scan QR -> camera-only capture
  -> upload). On areaMatchStatus "blocked" it shows "Area doesn't match /
  photograph the correct area and try again" and returns to the camera step
  for the SAME area scan (unlimited retries while the 90s window is valid);
  HTTP 503 shows "Verification unavailable"; "flagged" is invisible to
  staff. Camera-only via ImagePicker.launchCameraAsync (no gallery).

================================================================================

## 8. DEPLOYMENT

TWO parallel deploy paths coexist (unreconciled):

1. Manual / Docker Hub: root `docker-compose.yml` pulls
   `shumail47891/fyp:{backend,frontend}`, backend+frontend services only
   (the temporary `cv-service` was removed 2026-09-03), external
   DATABASE_URL, PUBLIC_HOST=161.118.180.138 (Oracle VM), frontend on
   :5173, COOKIE_SECURE=false.
2. CI/CD / GHCR: `.github/workflows/` — ci.yml (build/typecheck all apps +
   image builds), publish-images.yml (push cleanops-* images to GHCR),
   deploy-compose.yml (SCP `docker-compose.deploy.yml` + env to host,
   `docker compose pull && up -d`). Deploy compose self-hosts Postgres 16,
   binds backend to 127.0.0.1, defaults COOKIE_SECURE=true, frontend :80,
   optional mobile-web service (:8081).

- Backend Dockerfile: multi-stage node:22; CMD runs
  `prisma migrate deploy && node dist/app.js` (the squashed baseline made
  migrate deploy safe again: one migration, already marked applied).
- Frontend/mobile-web: nginx SPA + `location /api/ { proxy_pass
  http://backend:8080/api/; }`.
- MIGRATION RUNBOOK (updated 2026-09-02, TESTED):
  - The direct host `db.<ref>.supabase.co:5432` is IPv6-ONLY and this
    machine has no IPv6 route — DO NOT use it from here (P1001).
  - The transaction pooler (6543, ?pgbouncer=true) is for APP RUNTIME only;
    it rejects Prisma DDL ("prepared statement s1 already exists").
  - For ALL Prisma CLI commands (migrate dev/deploy/resolve, db push) use
    the SESSION-MODE pooler: same host
    `aws-1-ap-southeast-2.pooler.supabase.com` port 5432, same user
    `postgres.<ref>`, no pgbouncer param, `?sslmode=require`.
  - `npx prisma migrate dev` now works (single baseline replays cleanly on
    the shadow DB). `migrate status` reports "Database schema is up to
    date!".
  - Note recorded in backend/.env.example.
- Area-match verification is a hosted API call (Diff All) — the backend
  needs `DF_ALL` in its env (Heroku config var / compose env). If missing,
  uploads return 503 "verification unavailable" (by design, never a false
  block).
- Supabase free tier auto-pauses after ~7 days idle (past P1001/ETIMEDOUT
  cause) — unpause from dashboard.
- DuckDNS (cleanops.duckdns.org) is configured OUTSIDE the repo.
- SSH to Oracle VM: `ssh -i /home/shumail/Downloads/ssh-key-2026-02-03.key
  ubuntu@161.118.180.138` (alt user `opc`).

### Inconsistencies to be aware of

- `.env.example` DATABASE_URL points at db.prisma.io, but the runtime DB is
  Supabase, and deploy compose self-hosts Postgres — three DB stories.
- Image registries split: Docker Hub (root compose defaults) vs GHCR (CI).
- Root package.json is a stale Heroku launcher (leftover).
- docker-compose.deploy.yml requires MOBILE_WEB_IMAGE; .env.example never
  defines it.

================================================================================

## 9. GIT STATE (as of 2026-09-03)

- Single branch `main`, single remote.
- All recent feature work (QR verification, area-match, Diff All swap,
  migration squash, scanner fixes) is committed on local main; pushing has
  historically been manual — CHECK `git status` / `git log origin/main..main`
  before assuming remote matches.
- The old 25 migration folders were deleted from the working tree as part
  of the baseline squash — the deletion shows up in git and is intentional
  (recoverable from git history if ever needed).
- Do NOT pull/merge remote main unless explicitly asked (remote has moved
  independently in the past).

================================================================================

## 10. KNOWN ISSUES / TECH DEBT

Security:
- No rate limiting anywhere (login endpoints brute-forceable).
- verifyJwt checks isActive for managers but NOT staff — deactivated staff
  access tokens stay valid until expiry.
- Mobile tokens in AsyncStorage, not SecureStore; cleartext HTTP allowed.
- Staff login returns tokens in the JSON body (XSS-exfiltratable on web).
- Both clients match refresh on the literal string "Access Token Expired".
- COOKIE_SECURE=false default in backend Dockerfile; login cookies omit
  sameSite (refresh/logout include it).
- Debug logging middleware left on POST /api/attendance/check-in; stray
  console.logs of staff/attendance data.

Correctness:
- Time-basis split: assignment.controller, attendance.controller
  (assignShiftToStaff), and Zod shift schemas use UTC clock minutes while
  taskTemplate/staff-edit/assignment-service use location-tz minutes.
- Grace-period drift: 5 min hardcoded in startTask/getTodayStatus vs
  env-configurable 10 min in the reassignment cron.
- getTodaysTasksForStaff lacks manager location scoping.
- signupManager creates company + admin non-transactionally.
- editLocationSchema doesn't actually make fields optional.
- ensureAssignmentsForToday scans ALL open instances, not just today's.
- Mobile date logic hardcoded to Asia/Karachi (contradicts per-location tz).
- Frontend: no 404; query-key fragmentation leaves some lists stale.
- getMyAttendance checks role === "MANAGER" but not "ADMIN".

Housekeeping:
- Daily task scheduler polls every 90s (should be per-location scheduling).
- Dist is not cleaned on build — stale files (e.g. old cvService.client.js)
  accumulate in backend/dist; harmless but confusing. Delete dist/ before
  builds when diagnosing weird behavior.
- Dead code: frontend common/ form components; legacy Gemini verification
  provider (imageVerification.service + gemini/openai providers) still used
  ONLY by the legacy single-image completeTask path.
- Manager-created staff with "No location" immediately disappear from that
  manager's Staff tab (location-less staff are admin-visible only).
- No EAS config; EXPO_NO_DEPENDENCY_VALIDATION=1 masks dep warnings.
- Root README missing; mobile README has stale paths (/home/shumail/FYP/...).

================================================================================

## 11. SAFE COMMANDS

Local backend:   `cd backend && npm run dev`   (compiled dist; restart if stale)
Local frontend:  `cd frontend && npm run dev`   (open http://127.0.0.1:5173)
Backend tests:   `cd backend && npm run test`   (Vitest, 34 tests)
Backend build:   `cd backend && npm run build`
Frontend build:  `cd frontend && npm run build`
Mobile:          `cd frontend-mobile && npx expo start --clear` (LAN)
Mobile checks:   `cd frontend-mobile && npm run typecheck && npm run build:web`
Typecheck:       `npx tsc --noEmit` in backend/ and frontend/
Prisma CLI:      session-mode pooler URL (port 5432, see section 8 runbook):
                 `cd backend && DATABASE_URL="postgresql://postgres.<ref>:<pw>@aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres?sslmode=require" npx prisma migrate dev --name <name>`
                 (run ONLY when explicitly approved; migrate status /
                 migrate resolve are read-only/bookkeeping and safe)

================================================================================

## 12. FEATURE (2026-08-29): QR + TIME-WINDOW AREA VERIFICATION (hard gates)

Implements the deterministic anti-fraud gates. Backend:
- `POST /:taskId/start?qrToken=` — validates QR belongs to the task's
  template, task assigned to this staff, not completed/expired; records
  startedAt.
- `POST /:taskId/area/:referenceImageId/scan` — validates same task/QR,
  upserts the area's TaskAreaSubmission (scannedAt = server clock), starts
  the per-area window.
- `POST /:taskId/area/:referenceImageId/upload` — validates the window
  (AREA_UPLOAD_WINDOW_SECONDS = 90, `areaSubmission.service.ts`), then
  Cloudinary upload, then area-match verification (section 13), then
  APPROVED/BLOCKED/CV_ERROR.
- `completeTask` multi-area branch: task completes only when EVERY area has
  an APPROVED submission; proofImageUrls assembled from approved area
  photos. QR and time-window checks are server-side only (client timers are
  advisory).
- Retry semantics (`canAreaBeUploaded`): APPROVED -> locked; REJECTED_TIMEOUT
  -> re-scan QR to retry; BLOCKED / CV_ERROR -> re-upload directly on the
  same scan while the window is valid. Tests in
  backend/src/services/areaSubmission.service.test.ts.
- Mobile: per-area stepper in CompleteTaskScreen + QrScannerScreen
  (referenceImageId param routes to the area-scan endpoint; onScanSuccess
  callback advances the stepper without refetching).

## 13. FEATURE (2026-09-03): AREA-MATCH VERIFICATION (visual layer)

Second layer, after QR + time-window pass. Answers ONLY "does the staff
photo show the expected area?" — cleanliness is a separate, still-unbuilt
pipeline. Provider history: classical-CV Python sidecar (built 09-02,
ORB/SSIM/histogram) -> REPLACED 09-03 by the hosted Diff All API; the
`cv-service/` directory was deleted.

CURRENT PROVIDER (backend/src/services/verification/diffAll.client.ts):
- `POST https://diff-all.com/api/v1/compare`, header `X-API-Key: $DF_ALL`,
  multipart `file1` (staff photo, straight from the multer memory buffer),
  `file2` (reference image downloaded from Cloudinary), `mode=feature`
  (rotation/lighting tolerant). One retry on network/5xx, 15s timeout.
- Response `{status, similarity (0-100), type, verdict}` -> best score
  across ALL references of the area (same-name reference images = angles);
  `similarityScore = similarity/100`.
- Classification (env-configurable, percent scale):
  DIFFALL_MATCH_THRESHOLD (default 50), DIFFALL_BLOCK_THRESHOLD (default 28):
  >= match -> passed (flag false); >= block -> flagged (manager review,
  staff not informed); < block -> blocked (area NOT completed, staff sees
  "Area doesn't match", unlimited retries on the same scan).
  NOTE (2026-09-03 calibration): owner-set bars. mode=feature scores genuine
  same-scene captures 22.4-54.4 and clearly-wrong areas 6.3-13.2; genuine
  photos below 28 (hall 23.1, table 22.4) block, and the <28 zone cannot
  separate genuine re-framed from wrong-but-similar (~22.5) photos. See the
  regression test in diffAll.client.test.ts for measured data points.
- CV outage / missing key / all references undownloadable -> 503 +
  TaskAreaSubmission.status = CV_ERROR + attempt logged. A service failure
  is NEVER converted into a blocked verdict.
- Component score columns (color/ssim/feature) and weight columns stay in
  the schema but are stored NULL (Diff All returns a single score);
  similarityScore + thresholds used are always recorded; every attempt
  appends to `attempts` (audit for future threshold calibration).
- Pure classification logic extracted as `classifySimilarity` with vitest
  coverage (diffAll.client.test.ts).

MANAGER VISIBILITY: GET /:taskId/area-submissions + the "Areas" expandable
panel in the web dashboard (see section 6).

MOBILE UX: blocked -> exact spec message + back to camera step (same area,
no re-scan); 503 -> "Verification unavailable"; flagged invisible.

## 14. SUPERSEDED: earlier verification implementations (history only)

- 2026-08-18 single-reference AI verification (Gemini/OpenAI provider,
  location + cleanliness scores vs LOCATION_MATCH_THRESHOLD /
  CLEANLINESS_THRESHOLD, TaskCompletionAttempt rows) — still active ONLY
  for legacy single-image tasks (completeTask legacy branch). Multi-area
  tasks no longer use it.
- 2026-08-18 multi-area reference verification — superseded by section 12's
  QR + time-window flow (the old "upload all area photos on /complete with
  AI per-area check" flow was removed).
- 2026-09-02 classical-CV sidecar (FastAPI + ORB/RANSAC/SSIM/histogram,
  weights 15/25/60, thresholds 0.75/0.55) — replaced by Diff All 09-03.

================================================================================

## 15. HANDOFF PRIORITY

1. Read this file.
2. `git status` / `git log --oneline -10` — check branch and unpushed work.
3. Decide task type: local runtime / Oracle deploy / frontend UX / backend
   performance / security hardening / QR verification / area-match
   verification (Diff All) / cleanliness pipeline (NOT built yet).
4. For DB work, read the migration runbook in section 8 FIRST (pooler
   ports changed everything that was written before 2026-09-02).
5. Do not pull remote main; do not run migrations without explicit approval.
