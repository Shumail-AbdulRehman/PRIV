{
  "name": "mern-heroku-backend-launcher",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-prebuild": "npm install --prefix backend",
    "heroku-postbuild": "npm run build --prefix backend",
    "start": "npm start --prefix backend"
  }
}
